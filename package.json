{
  "name": "nest-multivendor-marketplace",
  "version": "2.0.0",
  "private": true,
  "description": "Arabic multi-vendor e-commerce marketplace MVP with owner, vendor and customer dashboards.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check assets/common.js"
  },
  "engines": {
    "node": ">=22"
  }
}
